{
  "_comment": "Postgres databases exposed via Aspire Resource MCP proxy (WithPostgresMcp). Aspire must be running: cd src/Airbnb.AppHost && dotnet run. See docs/Aspire_Postgres_MCP.md.",
  "mcpServers": {
    "aspire": {
      "type": "stdio",
      "command": "aspire",
      "args": ["agent", "mcp"]
    },
    "gitnexus": {
      "type": "stdio",
      "command": "npx",
      "args": ["gitnexus", "mcp"]
    }
  }
}
